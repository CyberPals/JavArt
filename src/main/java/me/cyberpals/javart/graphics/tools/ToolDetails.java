package me.cyberpals.javart.graphics.tools;

public enum ToolDetails {
    // move
    MOVE,
    MOVE_OBJECT,
    RESIZE,

    // shape
    OVAL,
    RECTANGLE,
    RHOMBUS,
    TRIANGLE,

    // combine
    DIFERENCE,
    INTERSECT,
    UNION,
    XOR,

    // one click
    SELECT,
    REMOVE,
    COPY,
    UNGROUP,
    SAVE,
    LOAD,
    RMI_SAVE,
    RMI_LOAD
}
